* Add formal definition for equilibrium/steady-state solutions.
* Add brackets to all instances of \ln so it's clear that ln is the name of a function.
* Search tex diagrams for cycle and replace with rectangle tikz command where appropriate.
* Add math notation patterns to both summary of v. transformations and summary of h. transformations.
* Find examples where an xref to pattern-integ-rebase would be appropriate.
* domain vs range in compositions of functions
  --> see written lecture notes
  ^^^^^^ I don't know what this is referring to anymore...
* first chapter needs more on linear models
  --- specifically, examples of determining whether data points could fit a linear
      model or not.
* using u for \ccmint{a}{t}{r(u)}{du} was a bad idea? Now it interferes with u for substitution...
* add a \jint{0}{c}{r(t)}{t} version of \jint{0}{t}{r(u)}{u} formulas.
* More clarity and maybe examples for negative domains of power function, both for m>0 and m<0.
* Maybe make a general pattern of horizontal shift/scale/reflection for definite integrals in the integ chapter,
  --> then discussion in the log chapter can be simplified.
  --> discussion of int(cos) can be simplified too?
  ^^^^ This is probably too much work and too complicated
* Add statements about continuity to poly, log, exp, trig.
* Example with title
  <title>A restricted domain could restrict the range</title>
  in funcs.ptx:
  consider both t^3 and t^2
  --- restricting domain for one restricts range,
      restricting domain for the other does not.
* rate of growth comparison to polynomial in both exp and log chapters
  (with reference to relevant section in inf-behaviour)
* relative growth: "of at most the order of" in periodic chapter
* fill opacity on graphs with grid lines
* fill opacity everywhere instead of using blue!40!white or similar
* some "Pattern" statements (<identity> element) might be better as "Principle" statements (<insight> element) ?
* should table-fund-thm-integrals in fund-thm.ptx be a <table> ?
